# Python workshop snippets

These are plain Python equivalents of the notebooks. Open in VS Code and use `# %%` cells, or run them from the repository root after copying `.env.workshop.example` to `.env`.

```powershell
.\.venv\Scripts\python.exe notebook_snippets\01_azure_openai_embeddings.py
```
